export const metadata = {
  title: "Login - Dipisha's Dashboard",
  description: "Login page for the admin dashboard.",
};

export default function LoginLayout({
	children,
}: {
	children: React.ReactNode;
}) {
	return <>{children}</>;
}